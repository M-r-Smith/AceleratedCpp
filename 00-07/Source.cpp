/*
0-7
What about this one?
*/

#include <iostream>
using namespace std;

int main()
{
	  /*
	  This is a comment that extends over several lines because it uses (/* and */) as its startingand ending delimiters* /

	  cout << "Does this work?" << endl;
	  return 0;
}

/*
The only reason that this does not work is because the example given ends the comment too soon.
However, if this was anyother comment this would work because you can place a comment anywhere
*/